container: {
    flex: 1,
    backgroundColor: 'transparent',
    alignItems: 'flex-start',
    justifyContent: 'center',
    flexWrap: 'wrap',
  }

Load state from Async storage in wifi mode/4g but get updates for switches on wifi and everything else on 4g

Save data continously in Async storage for each state change

get wifi details like bssid connected 

should get hub ip address from an backend api

layout issues on samsung devices to be fixed

panresponder should handle the sliding changes and save it to switches